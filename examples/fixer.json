[{
  "name": "Example of a test that passes.",
  "calls": [
    {
      "args": {
        "currency": "USD"
      },
      "on" : {
        "url" : "http://api.fixer.io/latest",
        "method":"GET",
        "params": {
          "base": "{currency}"
        },
        "headers": {
          "accept": "application/json"
        }
      },
      "expect": {
        "statusCode": 200,
        "contentType": "application/json",
        "body" : {
          "base": "USD"
        }
      }
    }
  ]
},
{
  "name": "Example of a test that does not pass.",
  "calls": [
    {
      "on" : {
        "url" : "http://api.fixer.io/latest",
        "method":"GET",
        "params": {
          "base": "USD"
        },
        "headers": {
          "accept": "application/json"
        }
      },
      "expect": {
        "statusCode": 200,
        "contentType": "application/json",
        "body" : {
          "base": "EUR"
        }
      }
    }
  ]
}]